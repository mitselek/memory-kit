# urls

One URL per file, on line 1, nothing else. The filename is the short name, cited from any
tier as `urls/<short-name>`.

URLs never go inline elsewhere, and a URL repeated in twelve records has twelve places to rot.
